[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "pdga-scraper"
version = "0.1.0"
description = "PDGA stats scraper"
requires-python = ">=3.8"

[tool.setuptools.packages.find]
where = ["."]
include = ["pdga_scraper*"]
